# Mascot Lottie canary — screenshots

Review assets for [PR #2728](https://github.com/peanutprotocol/peanut-ui/pull/2728). Images only; no application code.

This branch exists so the screenshots render inline in the pull request. It is never merged and nothing imports from it.

- `shots/sheet-1..3.png` — before/after pairs, WebP on the left and Lottie on the right, same route and viewport
- `shots/hero-overlap.png` — the landing hero at 430 / 375 / 320, the one composition call left for a human

Captured with Playwright against local builds of `origin/dev` and `feat/mascot-lottie`.
