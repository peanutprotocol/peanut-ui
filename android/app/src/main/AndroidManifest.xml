<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- App Links for peanut.me. Scoped to in-app routes via exact path +
                 trailing-slash prefix so web-only siblings (e.g. /send-money-to,
                 /pay-with) and marketing/help/locale pages still open in the browser.
                 Requires https://peanut.me/.well-known/assetlinks.json to verify. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="https" android:host="peanut.me" />

                <data android:path="/home" />
                <data android:pathPrefix="/home/" />
                <data android:path="/card" />
                <data android:pathPrefix="/card/" />
                <data android:path="/claim" />
                <data android:pathPrefix="/claim/" />
                <data android:path="/pay" />
                <data android:pathPrefix="/pay/" />
                <data android:path="/pay-request" />
                <data android:pathPrefix="/pay-request/" />
                <data android:path="/send" />
                <data android:pathPrefix="/send/" />
                <data android:path="/request" />
                <data android:pathPrefix="/request/" />
                <data android:path="/qr" />
                <data android:pathPrefix="/qr/" />
                <data android:path="/add-money" />
                <data android:pathPrefix="/add-money/" />
                <data android:path="/withdraw" />
                <data android:pathPrefix="/withdraw/" />
                <data android:path="/receipt" />
                <data android:pathPrefix="/receipt/" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true"
            tools:replace="android:authorities">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"
                tools:replace="android:resource" />
        </provider>
            <!-- CapacitorPasskey auto-configuration -->
        <meta-data
            android:name="asset_statements"
            android:resource="@string/capacitor_passkey_asset_statements" />
    </application>

    <!-- Phone-only: require a telephony radio so Play filters out Wi-Fi-only
         tablets, Android XR headsets, Chromebooks, TV/Wear/Auto. No telephony
         permission is needed — the feature flag alone drives Play's device
         catalog. NOTE: a cellular tablet that reports a telephony radio can
         still slip through; that residue is handled in Play Console device
         exclusions. -->
    <uses-feature android:name="android.hardware.telephony" android:required="true" />

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" tools:node="remove" />
    <!-- Android 13+ runtime push permission; OneSignal's requestPermission() drives the prompt -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
</manifest>
