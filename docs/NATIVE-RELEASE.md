# Native (Android) — Local Dev, Release & Play Review

This now lives in the mono repo at `engineering/native/release-android.md`.

Local dev and emulator setup now live in `engineering/native/agency-onboarding.md`.

Moved so the native runbooks have a single source of truth.
