import { CreateRoom } from '@/components/Split/CreateRoom'

export const metadata = {
	title: 'Split a trip · Peanut',
	description: 'Make a room, share the link, split expenses as you go. No signup.',
}

export default function CreateRoomPage() {
	return <CreateRoom />
}
