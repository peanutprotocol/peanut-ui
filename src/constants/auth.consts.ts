// Durable passkey credential markers a device keeps after logout; their
// presence is what makes /setup offer Log In before signup.
export const WEB_AUTHN_COOKIE_KEY = 'web-authn-key'
export const USER_PREFERENCES_KEY_SUFFIX = ':user-preferences'
