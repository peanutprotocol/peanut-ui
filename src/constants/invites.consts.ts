/**
 * Shown when a typed inviter username doesn't resolve to a Peanut member who
 * can invite. Shared by both waitlist gates (setup flow + jail screen) so the
 * copy can't drift between them.
 */
export const INVITER_NOT_FOUND_ERROR = "No Peanut member with that username. Double-check your inviter's exact handle."
