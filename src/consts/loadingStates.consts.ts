export type LoadingStates =
  | "idle"
  | "loading..."
  | "executing transaction..."
  | "allow network switch..."
  | "processing network switch..."
  | "checking signer..."
  | "checking inputs...";
