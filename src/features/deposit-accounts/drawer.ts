/**
 * vaul's close animation. A sheet a drawer hands off to (verification, support,
 * the residence change) opens after it: a sheet opened under the closing
 * overlay can miss its first tap.
 */
export const DRAWER_CLOSE_MS = 500
