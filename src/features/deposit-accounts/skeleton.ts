/**
 * The placeholder fill for deposit-account skeletons.
 *
 * Interim tint: no skeleton surface token exists yet (design.md open
 * conflict). Never bg-border-default — that is a near-black border color. The
 * pulse is decorative, so it stops for users who ask for reduced motion.
 */
export const SKELETON_PULSE = 'animate-pulse rounded bg-foreground-primary/10 motion-reduce:animate-none'
