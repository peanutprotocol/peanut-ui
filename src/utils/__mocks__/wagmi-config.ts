/**
 * Mock for @/config/wagmi.config
 */

export const networks = []

// Mock ContextProvider component
export const ContextProvider = ({ children }: { children: any; cookies: string | null }) => children
